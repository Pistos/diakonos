#include <stdio.h>

int main() {
    printf( "Hello World!\n" );

    if( condition ) {
        f();
    }

    if( condition )
        g();

    if( ql < xm ) {
        if( l_node != NULL )
            ret += l()->get( ql, xl , xm ) ;
    } else
        if(  r_node != NULL )
            ret += r()->get( ql , xm , xr ) ;

    h();

    return 0;
}
